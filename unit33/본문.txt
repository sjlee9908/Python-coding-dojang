변수의 사용범위, 클로저

전역변수
함수를 포함한 스크립트 전체에서 접근할 수 있는 변수

전역범위
전역변수에 접근할 수 있는 범위

지역변수
함수안에서 만들어진 변수

지역범위
지역변수에 접근할 수 있는 범위

함수 안에서 전역변수 값 변경
global 전역변수 
선언 후
전역변수 사용

네임스페이스
변수가 저장되는 곳
전역범위에서 local() 호출 - 전역 네임스페이스 딕셔너리 출력
지역범위에서 local() 호출 - 지역 네임스페이스 딕셔너리 출력

현재 함수보다 바깥쪽에 있는 지역변수의 값 변경
nonlocal 지역변수
선언후
현재 함수보다 바깥쪽에 있는 지역변수 사용
순서 - 가까운 함수부터 지역변수를 찾고, 없으면 계속 바깥쪽으로 나가서 찾음

클로저
함수를 둘러싼 환경(지역변수, 코드)등을 계속 유지하다가, 함수를 호출할 때 다시 꺼내서 사용하는 함수
def 함수1():
	지역변수1
	지역변수2
	def 함수2():
		return 함수2

